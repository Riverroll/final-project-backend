class Sales {
  constructor(salesId, salesName, createdAt, updatedAt) {
    this.salesId = salesId;
    this.salesName = salesName;
    this.createdAt = createdAt;
    this.updatedAt = updatedAt;
  }
}

module.exports = Sales;
